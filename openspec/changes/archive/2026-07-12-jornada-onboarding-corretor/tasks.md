# Tasks — Jornada: onboarding do corretor (#189)

## 1. Docs

- [x] 1.1 D-062 + `DECISIONS.md` mirror — `corretor.status` gate on
      D-055's role model, unified staff management page, dropped push
      notification (reconsidered against #192's precedent), per-house
      association folded in from draft §4.2 (previously unowned),
      staff-uploads-signed-contract-as-approval, **reopens
      `crm-source-of-truth`** for the broker-attribution constraint
      (logged explicitly, same treatment as #187's D-053 reopening)
- [x] 1.2 `docs/planning/templates/jornada-onboarding-corretor.md` —
      status lifecycle, staff management page contract, per-house flow
      detail, attribution constraint
- [x] 1.3 Update `docs/planning/jornadas-plataforma.md` §4.1 and §4.2 —
      mark validated, replace with corrected flow detail; merge §4.2 into
      this leaf's scope explicitly
- [x] 1.4 Update `docs/planning/screen-map.md` — clear rascunho/novo
      status for `/corretor/onboarding/*`, `/staff/corretores`,
      `/corretor/casas/[id]/contrato` (and unify the former
      `/staff/casas-pendentes` row into `/staff/corretores`)
- [x] 1.5 Sync `journey-corretor-onboarding` (new) and `crm-source-of-truth`
      (modified) specs; `openspec validate`; archive; merge `Closes #189`
