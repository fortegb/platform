## Context

Grilling #160 locked contract+slots, per-vendor rules, secrets/ownership, DoD.

## Goals / Non-Goals

**Goals:** D-039 + safe-targets template.

**Non-Goals:** Creating HubSpot/Tuya/WA accounts; committing tokens; inventing IDs.

## Decisions

1. Spec now, provision later (TBD slots).
2. Hard must-not per vendor (esp. Tuya ≠ sale house lock).
3. Secrets never in git/docs HTML.

## Risks

- **[Risk] Slots stay empty forever** → Mitigation: STATUS/next points to fill at setup; v1 vendors prioritized in map.
