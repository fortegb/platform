## 1. Fix

- [x] 1.1 `components/AppFooter.vue` — replace `hover:text-primary` with `hover:text-primary-100` on the "Links Rápidos" and "Legal" link lists.
- [x] 1.2 Verify in the browser: hover each footer link, confirm text is now visibly lighter than the `bg-primary-500` background.
