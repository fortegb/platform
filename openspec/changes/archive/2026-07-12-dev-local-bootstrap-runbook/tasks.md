## 1. Docs

- [x] 1.1 Add D-050 to `docs/planning/decisions.md` (+ mirror in
      `DECISIONS.md`): runbook scope (local only), location.
- [x] 1.2 Create `docs/planning/templates/dev-local-bootstrap.md` — ordered
      runbook referencing D-049 (toolchain), D-032 (Supabase local), D-044
      (env vars).
- [x] 1.3 Update pointer in `environments.md`.
- [x] 1.4 Update `STATUS.md` (`PRÓXIMO` → next leaf) and `CHANGELOG.md`.

## 2. Close-out prep

- [ ] 2.1 Human validation of docs.
- [ ] 2.2 Archive → merge `feat/*` → `main` directly (`origin/staging`
      still doesn't exist) with `Closes #171`.
